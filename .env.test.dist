# Overlay on .env / .env.dist (loaded first; only these keys differ).
NODE_ENV=test
POSTGRES_PRISMA_URL="postgresql://bilancarbone:bilancarbone@localhost:5433/bilancarbone_test"
NEXTAUTH_URL=http://localhost:3001
CYPRESS_URL=http://localhost:3001
